const environment = {};

environment.botResponseMessageTemplate = 'The door is open ${userName}';
environment.doorTimeout = 3000;
environment.relayGpioPort = 17;
environment.slackToken = "token";

export default environment;
